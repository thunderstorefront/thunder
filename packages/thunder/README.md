# Application template

## Commands

Development:

```bash
pnpm run dev
```

Generate codegen types:

```bash
pnpm run codegen
```

Build all packages:

```bash
pnpm run build
```

Codegen generate watch:

```bash
pnpm run codegen:watch
```